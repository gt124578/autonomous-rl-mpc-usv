# Training and evaluation only — the 3D validation additionally needs
# ROS Noetic + Gazebo 11 (system packages, not installable with pip).
numpy>=1.21,<2.0
scipy>=1.7
torch>=1.13
stable-baselines3>=2.0
gymnasium>=0.28
matplotlib>=3.5
pandas>=1.3
